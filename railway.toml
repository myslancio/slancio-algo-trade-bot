[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && gunicorn enterprise_project.asgi:application --bind 0.0.0.0:$PORT -k uvicorn.workers.UvicornWorker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
